.header {
  padding: 10px;
  height: 50px;
  color: lightblue;
  font-size: rem(20);
  background-color: darkslategrey;

  &-menu {
    margin: 0;
    list-style: none;

    li {
      display: inline-block;
      margin-right: em(10);
    }

    a {
      color: white;
    }
  }
}
